var subject = "CS"; // computerscience, IT, CS

if (subject === "physics") {
  console.log(`go to ${subject} department`);
} else if (subject === "CS") {
  console.log(`go to ${subject} department`);
} else if (subject === "history") {
  console.log(`go to ${subject} department`);
} else {
  console.log("check the department");
}
